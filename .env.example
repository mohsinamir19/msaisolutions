# Populate these with the real environment variables used by the app.
# Do not commit real values.

# Base application URL for previews, production, and absolute links.
NEXT_PUBLIC_SITE_URL=

# Add server-side secrets here once the app defines them.
# Example:
# DATABASE_URL=
# AUTH_SECRET=
